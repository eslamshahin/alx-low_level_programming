#include <stdio.h>
/**
 * main - Entry - point
 *
 */

int main(void)
{
	char c;

	int d;

	c = 'a';
	d = 0;
	while
		(d < 10) {
			putchar(d + "0");
			d++;
		}
	while
		(c <= 'F') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
